export enum EventType {
    TABLE_INIT_OVER = "table_init_over",
    HERO_ATTACK = "hero_attack",
    HERO_ATTACK_OVER = "hero_attack_over",
};

export const enum CAMPS {
    LEFT,
    RIGHT
};

export const MAX_ENERGY = 100;
export const MAX_DEFENCE = 100;
export const HERO_COUNT = 5;